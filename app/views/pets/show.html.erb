<h1> <%= @pet.name %> </h1>

<p> <%= image_tag @pet.image %></p>
<p> <%= @pet.description %></p>
<p> <%= @pet.approx_age %></p>
<p> <%= @pet.sex %></p>
<p> <%= @pet.shelter.name %></p>
<p> <%= @pet.status %></p>

<%= link_to "Update Pet", "/pets/#{@pet.id}/edit" %>
<%= link_to "Delete Pet", "/pet/#{@pet.id}", method: :delete %>
